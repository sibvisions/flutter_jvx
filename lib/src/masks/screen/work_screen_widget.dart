import 'package:flutter/material.dart';

class WorkScreenWidget extends StatelessWidget {
  const WorkScreenWidget({Key? key, required this.screenClassName}) : super(key: key);

  final String screenClassName;





  @override
  Widget build(BuildContext context) {
    return (Scaffold(

    ));
  }
}
