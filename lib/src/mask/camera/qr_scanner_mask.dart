import 'package:flutter/material.dart';

class QRScannerMask extends StatelessWidget {
  const QRScannerMask({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("QR Scanner"),),
    );
  }
}
